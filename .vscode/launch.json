{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Debug Chess GUI",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/gui/chess_gui.exe",
            "cwd": "${workspaceFolder}/build/gui",
            "MIMode": "gdb",
            "miDebuggerPath": "gdb",
            "preLaunchTask": "Build (Debug)"
            
        },
    ]
}