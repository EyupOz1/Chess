{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Debug Chess GUI",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Chess/gui/chess_gui.exe",
            "cwd": "${workspaceFolder}/build/Chess/gui",
            "MIMode": "gdb",
            "miDebuggerPath": "gdb",
            "preLaunchTask": "Build (Debug)"
            
        },
    ]
}
